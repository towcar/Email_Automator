<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.somethingelselabs.mail">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <!-- Set this to the loading screen -->
        <activity
            android:name="com.somethingelselabs.mail.activities.HomeActivity"

            android:screenOrientation="portrait"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.somethingelselabs.mail.activities.MailFolderActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name="com.somethingelselabs.mail.activities.MailViewActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name="com.somethingelselabs.mail.activities.AutomateActivity"
            android:screenOrientation="portrait"/>

    </application>


</manifest>